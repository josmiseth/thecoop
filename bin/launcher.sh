#/bin/sh
# launcher.sh
# navigate to home directory, then to the source directory, then execute the python script, then back to home
cd /
cd home/josmi/projects/thecoop/src
sudo python hatch_controller.py
cd /
